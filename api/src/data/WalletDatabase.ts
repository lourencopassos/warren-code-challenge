import { TransactionInputDTO } from "../model/Statement";
import { BaseDatabase } from "./BaseDatabase";

export class WalletDatabase extends BaseDatabase {

  saveTransaction = async (transaction: TransactionInputDTO) => {
    try {

    } catch (error) {

    }
  }

}